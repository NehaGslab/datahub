source:
  type: kafka
  config:
    connection:
      bootstrap: "localhost:9092"
      consumer_config:
        security.protocol: "PLAINTEXT"
    topic_patterns:
      allow:
        - "raw_orders"
        - "source\\.raw_orders"
    env: PROD

